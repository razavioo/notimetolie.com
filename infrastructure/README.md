# No Time To Lie - Infrastructure

This directory contains infrastructure configuration files for the No Time To Lie project.

## Docker Configuration

The `docker/` directory contains Docker files and Docker Compose configurations for running the application locally.

## Terraform Configuration

The `terraform/` directory contains Infrastructure as Code (IaC) configurations for deploying the application to cloud environments.